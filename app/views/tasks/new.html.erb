<% content_for :title, "New task — #{@project.name}" %>
<h1 class="text-xl font-semibold text-[color:var(--color-ink)] mb-4">New task in <span class="font-mono text-[color:var(--color-ink-soft)]"><%= @project.slug %></span></h1>
<%= render "form", task: @task %>
