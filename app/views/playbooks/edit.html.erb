<% content_for :title, "Edit playbook" %>
<div class="mb-4">
  <div class="text-xs text-[color:var(--color-ink-soft)] font-mono"><%= link_to @project.name, @project, class: "hover:underline" %> / <%= link_to "playbooks", project_playbooks_path(@project), class: "hover:underline" %> / edit</div>
  <h1 class="text-xl font-semibold text-[color:var(--color-ink)]">Edit playbook</h1>
</div>
<%= render "form", playbook: @playbook %>
