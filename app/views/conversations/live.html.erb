<%# The "Live now" frame. The index subscribes to the "live_sessions" Turbo Stream
    and renders this frame with a slow auto-reload backstop; broadcasts replace the
    inner strip the instant a session syncs a turn. %>
<%= turbo_frame_tag "live_sessions" do %>
  <%= render "conversations/live_strip", live_conversations: @live_conversations %>
<% end %>
