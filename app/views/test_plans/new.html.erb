<% content_for :title, "New test plan" %>
<h1 class="text-xl font-semibold text-[color:var(--color-ink)] mb-4">New test plan in <span class="font-mono text-[color:var(--color-ink-faint)]"><%= @project.slug %></span></h1>
<%= render "form", plan: @plan %>
