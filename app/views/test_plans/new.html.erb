<% content_for :title, "New test plan" %>
<h1 class="text-xl font-semibold text-slate-900 mb-4">New test plan in <span class="font-mono text-slate-500"><%= @project.slug %></span></h1>
<%= render "form", plan: @plan %>
